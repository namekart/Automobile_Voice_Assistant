# LiveKit voice agent
livekit-agents
livekit-plugins-openai   # LLM (OpenRouter) + optional OpenAI TTS
livekit-plugins-silero  # VAD (voice activity detection)
livekit-plugins-turn-detector  # End-of-turn detection (multilingual)
livekit-plugins-deepgram # STT (low-latency speech-to-text)
livekit-plugins-sarvam  # STT/TTS for Indian languages (Sarvam AI)
livekit-plugins-google  # LLM (Gemini) - set GOOGLE_API_KEY in .env
livekit-plugins-groq    # LLM (Groq, e.g. gpt-oss-120b) - set GROQ_API_KEY in .env
livekit-plugins-elevenlabs  # TTS (ElevenLabs) - set ELEVEN_API_KEY in .env (commented in agent)
livekit-plugins-cartesia   # TTS (Cartesia sonic-3, Ishan voice) - set CARTESIA_API_KEY in .env
livekit-plugins-anthropic  #Fallback LLM
python-dotenv           # load .env (LIVEKIT_*, OPENROUTER_API_KEY)
livekit-plugins-noise-cancellation
asyncpg                 # Async PostgreSQL for CRM (non-blocking, low latency)